name: Ruff Linting

on: [push, pull_request]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Intall python
      - uses: actions/setup-python@v5
     